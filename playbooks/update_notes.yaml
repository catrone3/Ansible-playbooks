- hosts: all

  become: yes
  tasks:
    - name: Clone shadowrun notes
      ansible.builtin.git:
        repo: "https://github.com/catrone3/shadowrunNotes.git"
        dest: /opt/vaults/Shadowrun
        update: yes
